import 'package:flutter/material.dart';

class CrearRecordatorio extends StatelessWidget {
  const CrearRecordatorio({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
